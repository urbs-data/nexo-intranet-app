import { BookingInfoContent } from '@/features/bookings/components/booking-info-content';

export const metadata = {
  title: 'Dashboard : Ver Reserva'
};

export default function Page() {
  return <BookingInfoContent />;
}
