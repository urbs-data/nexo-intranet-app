import ProviderNewPage from '@/features/providers/components/provider-new-page';

export const metadata = {
  title: 'Dashboard : New Provider'
};

export default async function Page() {
  return <ProviderNewPage />;
}
